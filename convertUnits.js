const Converts = class {
  constructor(base, result){
    this.base = base;
    this.result = result;
  }

  from(fromUnit){

  }

  to(toUnit){

  }

}

const konversi = new Converts(8,9);
console.dir(konversi);
